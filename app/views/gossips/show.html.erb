<br>
<div class="container">
  <div class="card">
    <div class="card-header">
      <%= @gossip.title %> - Gossip n°<%= params[:id] %>
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p><%= @gossip.content %></p>
        <footer class="blockquote-footer"><%= link_to "#{@gossip.user.first_name}",{controller: "users", action: "show", id: @gossip.id} %> from <%= @gossip.user.city.name %> </footer>
      </blockquote>
    </div>
  </div>
  <br>
  
  <br>
  <a href="<%= edit_gossip_path %>" class="btn btn-secondary">Edit the Gossip</a>
  <a href="<%= gossips_path %>" class="btn btn-danger" data-confirm="Are you sure you want to delete this gossip?">Delete the Gossip</a>
</div>
