<br>
<div class="container">
  <div class="card">
    <div class="card-header">
      <%= Gossip.find(params[:id]).title %> - Potin n°<%= params[:id] %>
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p><%= Gossip.find(params[:id]).content %></p>
        <footer class="blockquote-footer"><%= link_to "#{Gossip.find(params[:id]).user.first_name}",{controller: "pages", action: "show_author", id: Gossip.find(params[:id]).id} %> <%= link_to "#{Gossip.find(params[:id]).user.last_name}" %> from <%= Gossip.find(params[:id]).user.city.name %> </footer>
      </blockquote>
    </div>
  </div>
  <br>
  <a href="<%= edit_gossip_path %>" class="btn btn-primary">Edit the Gossip</a>
  <a href="<%= gossips_path %>" class="btn btn-danger" data-confirm="Are you sure you want to delete this gossip?">Delete the Gossip</a>
</div>
